{% extends 'base.html.twig' %}

{% block title %}
	Post-{{post.title}}
{% endblock %}

{% block body %}

	<h3>{{post.title}}
	</h3>
	<div style="color:grey">
		{% if date(post.created)< date('-1year') %}
			Older than One year
		{% else %}
			{{post.created|date('d/m/y')}}
		{% endif %}
	</div>
	<h4>
		<a href="{{path('app_micro_post')}}">
			all list </a>
		</h4>

	{% endblock %}
